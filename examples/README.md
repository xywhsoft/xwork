# examples

This directory is reserved for runnable `xwork` examples.

Planned first examples:

- minimal runtime + workspace + run
- mock tool registry
- mock approval flow
- `xllm` orchestrator smoke
