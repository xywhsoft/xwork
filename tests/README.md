# tests

This directory is reserved for `xwork` tests.

Planned first test groups:

- core object lifecycle
- run state transitions
- workspace/tool registration
- mock orchestration flow
- checkpoint and resume
